import 'package:flutter/material.dart';

class AppColors {
  // Primary colors - Orange theme
  static const Color primary = Color(0xFFFF6B35); // Bright Orange
  static const Color primaryLight = Color(0xFFFF8A5C);
  static const Color primaryDark = Color(0xFFE55A24);

  // Background - Dark Brown Gradient
  static const Color backgroundStart = Color(0xFF2D1810); // Dark Brown
  static const Color backgroundEnd = Color(0xFF1A0E08); // Darker Brown
  static const Color background = Color(0xFF1A0E08);

  // Surface colors
  static const Color surfaceLight = Color(0xFF2A2A2A); // Dark surface
  static const Color surfaceDark = Color(0xFF1F1F1F); // Darker surface
  static const Color surfaceAlt = Color(0xFF333333);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF); // White
  static const Color textSecondary = Color(0xFFB8B8B8); // Light grey
  static const Color textHint = Color(0xFF888888); // Medium grey

  // Borders and dividers
  static const Color border = Color(0xFF444444); // Dark grey
  static const Color divider = Color(0xFF3A3A3A);

  // Status colors
  static const Color available = Color(0xFF10B981); // Green
  static const Color rented = Color(0xFFEF4444); // Red
  static const Color maintenance = Color(0xFFFFB800); // Amber
  static const Color pending = Color(0xFF6366F1); // Indigo

  // Utility colors
  static const Color error = Color(0xFFEF4444);
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFFFB800);
  static const Color info = Color(0xFF3B82F6);

  // Shadow colors
  static const Color shadowLight = Color(0x1A000000);
  static const Color shadowMedium = Color(0x2D000000);
  static const Color shadowDark = Color(0x4D000000);
}
